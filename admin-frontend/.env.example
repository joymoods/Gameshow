# URL des Backends (leer lassen wenn Caddy/gleiche Origin verwendet wird)
VITE_API_URL=http://localhost:8080

# URL des Player-Frontends – wird im QR-Code in der Lobby angezeigt
VITE_PLAYER_URL=http://localhost:5174

# PIN-Schutz für das Admin-Frontend (leer lassen = deaktiviert)
VITE_ADMIN_PIN=
